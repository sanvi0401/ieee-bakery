document.getElementById("orderForm").addEventListener("submit", function(event) {
    event.preventDefault(); // Prevent the default form submission
    alert("Order submitted!"); // You can replace this with actual order logic
});
